Place the preprocessed regional OSMCoastline land-polygon extract here as:

```text
land-polygons.json
```

The Worker also supports an externally hosted JSON file via `MARINE_LANDMASK_URL`.

Do not commit the full global OSMCoastline dataset. Trim it to the operating region first.
